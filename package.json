{
  "name": "vnet",
  "version": "1.0.1",
  "description": "Parse vnet packets and emit them as a binary stream",
  "main": "vnet.js",
  "scripts": {
    "test": "tap test/[^_]*.js",
    "lint": "jshint .",
    "validate": "npm ls"
  },
  "author": "Russell Hay",
  "license": "MIT",
  "devDependencies": {
    "precommit-hook": "^3.0.0",
    "stream-buffers": "^2.2.0",
    "tap": "^1.4.0"
  },
  "pre-commit": [
    "lint",
    "validate",
    "test"
  ],
  "dependencies": {
    "packet": "0.0.6"
  }
}
